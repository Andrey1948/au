spring:
  config:
    activate:
      on-profile: home
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/HomeDb
    username: postgres
    password: postgres